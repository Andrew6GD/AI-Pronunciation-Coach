# Azure Speech Service Configuration
# 在Netlify控制台的Environment Variables中设置这些变量
# 获取方式：Azure Portal -> Speech Service -> Keys and Endpoint

# Azure Speech Service API Key
AZURE_SPEECH_KEY=your_azure_speech_key_here

# Azure Speech Service Region (例如：eastus, westus2, eastasia)
AZURE_SPEECH_REGION=eastus

# Google Gemini API密钥（如果需要）
# 获取方式：https://makersuite.google.com/app/apikey
# GOOGLE_API_KEY=your_google_gemini_api_key_here

# 开发环境TTS端点（可选）
# 本地开发时如果需要使用不同的TTS端点
# VITE_TTS_ENDPOINT=http://localhost:8888/.netlify/functions/tts

# 注意事项：
# 1. 复制此文件为 .env 用于本地开发
# 2. 在Netlify控制台中设置环境变量用于生产环境
# 3. 不要将真实的API密钥提交到Git仓库中
# 4. VITE_开头的变量会暴露给前端，不要用于敏感信息
# 5. 只有AZURE_SPEECH_KEY和AZURE_SPEECH_REGION会在Netlify Functions中使用